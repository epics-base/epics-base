#define EPICS_VERSION        ${EPICS_VERSION}
#define EPICS_REVISION       ${EPICS_REVISION}
#define EPICS_MODIFICATION   ${EPICS_MODIFICATION}
#define EPICS_PATCH_LEVEL    ${EPICS_PATCH_LEVEL}
#define EPICS_CVS_SNAPSHOT   "${EPICS_CVS_SNAPSHOT}"
#define EPICS_SITE_VERSION   "${EPICS_SITE_VERSION}"
#define EPICS_VERSION_STRING "EPICS ${EPICS_VERSION_STRING}"
#define epicsReleaseVersion "EPICS R${EPICS_VERSION_STRING}"

/* EPICS_UPDATE_LEVEL is deprecated, use EPICS_PATCH_LEVEL instead */
#define EPICS_UPDATE_LEVEL    ${EPICS_PATCH_LEVEL}
