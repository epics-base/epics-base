EPICS = ../../../..
include Target.include
include $(EPICS)/config/CONFIG

#USR_CFLAGS = -DACCESS_SECURITY -D_NO_PROTO

SRCS = ../epicsEnvParams.c ../epicsRelease.c

OBJS = \

LIBOBJS = epicsEnvParams.o epicsRelease.o

LIBNAME = miscLib

all: install_libs

clean::
	rm -f miscLib

include $(EPICS)/config/RULES.Vx
