### Allow hex and octal stings in dbPut

It is now possible to write hex and octal strings to integer
fields with `dbPut()` and related functions.

Possible incompatibility: Up to now, leading `0`s have been ignored,
now they switch to octal mode.
