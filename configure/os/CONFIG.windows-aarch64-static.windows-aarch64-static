# CONFIG.windows-aarch64-static.windows-aarch64-static
#
# Definitions for windows-aarch64-static host - windows-aarch64-static target build
# Override these definitions in CONFIG_SITE.windows-aarch64-static.windows-aarch64-static
#-------------------------------------------------------

#Include definitions common to windows-aarch64 builds
include $(CONFIG)/os/CONFIG.windows-aarch64.windows-aarch64

# Override CONFIG.CrossCommon settings:
BUILD_CLASS = HOST

# Override CONFIG_SITE settings:
SHARED_LIBRARIES = NO
STATIC_BUILD = YES
